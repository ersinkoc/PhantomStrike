apiVersion: v1
kind: Service
metadata:
  name: {{ include "phantomstrike.fullname" . }}
  labels:
    {{- include "phantomstrike.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "phantomstrike.selectorLabels" . | nindent 4 }}
