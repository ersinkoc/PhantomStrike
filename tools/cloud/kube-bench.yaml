name: kube-bench
version: "1.0"
category: cloud/kubernetes-security
phase: scanning
command: kube-bench
args:
  - "run"
  - "--json"
docker:
  image: phantomstrike/kube-bench:latest
  memory_limit: "512m"
  timeout: "300s"

short_description: Kubernetes CIS benchmarks
description: |
  kube-bench is a tool that checks whether Kubernetes is deployed securely
  by running the checks documented in the CIS Kubernetes Benchmark.

parameters:
  - name: config
    type: string
    required: false
    flag: "--config"
    description: Kubeconfig file

output:
  format: json

enabled: true
